# Vimeo Player

Aplicación SwiftUI universal para iOS, iPadOS y macOS. Reproduce el enlace configurado en `VimeoPlayer/ContentView.swift` dentro de un `WKWebView` a pantalla completa.

Abre `VimeoPlayer.xcodeproj` con Xcode y selecciona un simulador, dispositivo iOS/iPadOS o **My Mac**.
